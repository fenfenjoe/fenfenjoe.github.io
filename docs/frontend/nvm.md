# nvm

```
# 查看nvm版本
nvm -v

# 安装指定版本nodejs
nvm install <version>

# 卸载指定版本nodejs
nvm uninstall <version>

# 使用指定版本nodejs
nvm use <version>

# 列出已安装版本
nvm list installed

# 查看当前使用中的nodejs版本
nvm current

# 查看所有命令说明
nvm -h
```